# node-ts-template
Project template for using Node.JS with TypeScript

### Using this template
```
mkdir project-name
cd project-name
gh repo clone jasonly027/node-ts-template . -- --depth=1
rm -rf .git/
```
